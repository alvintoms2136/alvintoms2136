# Required for the Control UI (Dashboard) login
CLAWDBOT_GATEWAY_TOKEN=replace-with-strong-token

# Recommended model provider
ANTHROPIC_API_KEY=your-anthropic-key

# Optional alternative provider
OPENAI_API_KEY=your-openai-key

# Gateway mode (default is local)
GATEWAY_MODE=local
